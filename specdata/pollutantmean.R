
pollutantmean <-function(directory, pollutant, id = 1:332){
  setwd(directory)
  files_list = file.names(directory, full.names = TRUE)
  for(number in id){
    for (x in mydata[[3]]){
      if(!is.na(x)){
        print(x)}
    }

  }
}